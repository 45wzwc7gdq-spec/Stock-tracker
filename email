const nodemailer = require("nodemailer");
const cfg = require("../config");

module.exports = nodemailer.createTransport({
    host: "smtp.office365.com",
    port: 587,
    secure: false,
    auth: {
        user: cfg.OUTLOOK_EMAIL,
        pass: cfg.OUTLOOK_PASS
    }
});
